# 2个对象数组相同属性值去重

---

``` javascript
//取出2个数组对象不同对象的数组
let diffList = arrayA.filter(item =>{return !arrayB.map(item => item.cityId).includes(item.cityId)}); 
//取出2个数组对象相同对象的数组
let sameList = arrayA.filter(item =>{return arrayB.map(item => item.cityId).includes(item.cityId)});
```
